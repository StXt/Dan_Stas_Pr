mixin navLink(link, name)
    li
      a(href=link).nav-link=name

header
    .header
        ul.nav
            +navLink('#','Home')
            +navLink('#','Link 1')
            +navLink('#','Link 2')